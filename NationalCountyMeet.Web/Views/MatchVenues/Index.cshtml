@model IEnumerable<NationalCountyMeet.Web.Models.MatchVenue>

@{
    ViewData["Title"] = "Index";
    Layout = "_SubLayout";
    int Counter = 1;
}

<div class="row">
    <div class="col-xl-12 col-lg-12 col-md-12 col-sm-12 col-12">
        <div class="card">
            <div class="card-header">
                <div class="row col-md-12">
                    <div class="col-6 col-md-6">
                        <h3>Match venues Listing</h3>
                    </div>
                    <div class="col-6 col-md-6 p-0 text-right">
                        <a asp-controller="MatchVenues" asp-action="AddNewMatchVenue" class="btn btn-primary btn-sm"><i class="fas fa-plus"></i>&nbsp;Add New</a>
                    </div>
                </div>
            </div>
            <div class="card-body">
                <div class="table-responsive">
                    <table id="example" class="table table-striped table-bordered second" style="width:100%">
                        <thead>
                            <tr>
                                <th>
                                    No.
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.VenueName)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.Location)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.Capacity)
                                </th>
                                <th class="text-right">
                                    Actions
                                </th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @String.Format("{0}", Counter++)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.VenueName)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Location)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Capacity)
                                    </td>
                                    <td class="text-right">
                                        <a asp-controller="MatchVenues" asp-route-id="@item.MatchVenueId" asp-action="MatchVenueDetails" class="btn btn-sm btn-light active"><i class="fas fa-eye"></i></a>
                                        <a asp-controller="MatchVenues" asp-route-id="@item.MatchVenueId" asp-action="EditVenueRecord" class="btn btn-sm btn-success"><i class="far fa-edit"></i></a>
                                        <a asp-controller="MatchVenues" asp-route-id="@item.MatchVenueId" asp-action="DeleteMatchVenue" class="btn btn-sm btn-danger"><i class="far fa-trash-alt"></i></a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>