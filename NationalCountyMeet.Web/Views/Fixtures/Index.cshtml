@model IEnumerable<NationalCountyMeet.Web.Models.Fixture>

@{
    ViewData["Title"] = "Index";
    Layout = "_SubLayout";
    int Counter = 1;
}

<div class="row">
    <div class="col-xl-12 col-lg-12 col-md-12 col-sm-12 col-12">
        <div class="card">
            <div class="card-header">
                <div class="row col-md-12">
                    <div class="col-6 col-md-6">
                        <h3>Fixtures Listing</h3>
                    </div>
                    <div class="col-6 col-md-6 p-0 text-right">
                        <a asp-controller="Fixtures" asp-action="CreateNewFixture" class="btn btn-primary btn-sm"><i class="fas fa-plus"></i>&nbsp;Add New</a>
                    </div>
                </div>
            </div>
            <div class="card-body">
                <div class="table-responsive">
                    <table id="example" class="table table-striped table-bordered second" style="width:100%">
                        <thead>
                            <tr>
                                <th>
                                    No.
                                </th>
                                <th>
                                    Fixture
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.MatchDate)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.StartTime)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.MatchVenueId)
                                </th>
                                <th class="text-right">
                                    Actions
                                </th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @String.Format("{0}", Counter++)
                                    </td>
                                    <td>
                                        @String.Format("{0} vs {1}", @item.HomeTeamId, @item.AwayTeamId)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.MatchDate)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.StartTime)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.MatchVenue.VenueName)
                                    </td>
                                    <td class="text-right">
                                        <a asp-controller="Fixtures" asp-route-id="@item.FixtureId" asp-action="CountyDetails" class="btn btn-sm btn-light active"><i class="fas fa-eye"></i></a>
                                        <a asp-controller="Fixtures" asp-route-id="@item.FixtureId" asp-action="EditCountyRecord" class="btn btn-sm btn-success"><i class="far fa-edit"></i></a>
                                        <a asp-controller="Fixtures" asp-route-id="@item.FixtureId" asp-action="DeleteCountyRecord" class="btn btn-sm btn-danger"><i class="far fa-trash-alt"></i></a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
